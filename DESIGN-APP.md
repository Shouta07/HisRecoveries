# His Recoveries Design System — `/app`

Relationship Companion（`/app` 以下）の見た目の規則。
既存20ルート（`/`, `/areas/*` など）は別系統で、色の名前も分けてある。
互いに触らない。

実装は `src/components/app/system.tsx`（部品）と `tailwind.config.ts`（トークン）。
**ここに無い見た目を画面側で直接書き始めたら、そこから崩れる。**

---

## 1. 原則

迷ったらここに戻る。

1. 人間関係を管理しない
2. 相手を評価しない
3. 自分を振り返る
4. 説明より先に触らせる
5. 一画面、一つの問い

この5つに反する部品は、この設計システムに存在しない。
進捗率・STEP n/m・スコア・成功率のコンポーネントは、作らない。

## 2. 色

| 名前 | 値 | 使う場所 |
|---|---|---|
| `ground` | `#FBF9F7` | 画面の地。ほぼここで埋まる |
| `surface` | `#FFFFFF` | 面。**選択肢と経験談だけ**。それ以外に使わない |
| `raised` | `#F4F0EC` | ごく稀。沈めたい帯 |
| `charcoal` | `#2A2622` | 問い・見出し |
| `bodytext` | `#5A534C` | 本文 |
| `faint` | `#8C8378` | ラベル・日付・補足 |
| `hairline` | `#EAE4DE` | 線 |
| `accent` | `#B2543C` | **1画面に1箇所** |
| `accent-tint` | `#F4EAE6` | 選択中の面 |

アクセントは1色だけ。地の上で 4.67:1、白文字で 4.86:1（どちらも AA）。
前の `#D9694F` は本文で 3.33:1 しかなく、リンク文字に使っていた。

グラデーション禁止。紫禁止。ピンク禁止。
2箇所目のアクセントが要るなら、その画面の設計が間違っている。

## 3. 文字

| 役 | サイズ / 行間 | 部品 |
|---|---|---|
| 問い | 26–28 / 1.5 | `<Ask>` |
| 見出し | 17 / 1.55 | `<Head>` |
| 本文 | 15 / 1.95 | `<Body>` |
| 補助 | 13.5 / 1.85 | — |
| ラベル | 11.5 / 0.12em | `<Label>` |
| 脚注 | 12.5 / 1.8 | `<Note>` |

画面でいちばん大きい文字は**必ず問い**。数字でも見出しでもない。
日本語を小さく詰め込まない。

## 4. 余白

`4 / 8 / 12 / 16 / 24 / 32 / 48 / 64` のみ。

- ブロック間 32（`mt-8`）
- セクション間 40–48（`mt-10` / `mt-11`）
- 画面の頭 24（`pt-6`）

## 5. 角丸・線・影

- 角丸 12（選択肢・経験談） / 8（ボタン・入力） / 999（チップ）
- 線 1px `hairline`。2px 以上は使わない
- **影は使わない**。浮かせずに、線と余白で分ける

## 6. カードの方針

区切りは **余白 → 線 → カード** の順で選ぶ。
カードを使ってよいのは次だけ。

- 選択肢（`<Choice>`）
- 経験談など、明確に独立した読み物
- 取り消しの確認のような、いま止まってほしいもの

記録の一覧・道のり・自分は、カードを使わない。
1件ずつを白い角丸に入れた瞬間、案件の一覧に見える。

## 7. 一本の線

このプロダクト全体を通した唯一のモチーフ（`src/components/app/Line.tsx`）。

- `<JourneyLine>` 道のり。横一本、点が8つ、いまいるところだけ大きい
- `<Node>` 記録の時系列。縦一本の線の上に点を置く
- `<Rule>` 区切り

**線は塗り分けない。** 左から右へ色を伸ばすと、右に行くほど良いという意味が生まれる。
恋愛には戻ることも、止まることもある。

## 8. 動き

180–260ms。使ってよいのは3つだけ。

| 名前 | 用途 |
|---|---|
| `animate-hr-rise` | 画面・設問の切り替え（fade + translateY 10px） |
| `animate-hr-fade` | 小さな状態変化 |
| `animate-hr-draw` | 線が引かれる |

弾ませない。光らせない。紙吹雪を出さない。
**必ず `motion-safe:` を付ける**（`prefers-reduced-motion` で全部止まる）。

## 9. 触る

- タップ領域 44px 以上
- 下タブは4つまで。中央に丸いボタンを浮かせない
- 一画面一問の最中（`/app/new`, `/app/start`）は下タブを出さない
- 入力中に主要操作がキーボードで隠れないよう、ボタンは通常フローに置く

## 10. 空のとき

`<Empty>` は「ありません」で止めない。
**なぜ空で、何をすると埋まるか**まで書く。ここの文章はこのプロダクトの人格そのもの。

```
まだ記録はありません。
会った日や、何か感じた日に残してみてください。
　最初の記録を残す →
```

```
まだ、決めつけません。
いくつか記録が増えると、あなた自身の傾向が少しずつ見えてきます。
```

## 11. 書かないこと

画面の文言に次が入ると、ビルドが落ちる。

| ファイル | 止めるもの |
|---|---|
| `src/lib/journey.ts` | 落とせ・攻略・テクニック・脈あり・必ず・絶対・してください |
| `src/lib/reflection.ts` | 脈・確率・成功率・相性・点・おすすめ・正解・相手はこう思って |
| `src/lib/today.ts` | 相手は・相手の気持ち・脈・好意・思われて・％ |
| `src/lib/relationship.ts` | 記録の型に name / nickname / photo / phone / line / sns / message / partner / contact（型エラー） |
| `src/lib/knowledge.ts` | 出どころの無い経験談・時点の無いデータ・形の壊れた受付番号 |

さらに `reflection.ts` は、迷いが濃い3段階（また会う／知る／関係を考える）に
**進まない選択肢が1つも無いと落ちる**。前に進むことだけを正解にしないため。

## 12. 画面

```
今日 /app            記録 /app/record     知る /app/knowledge   自分 /app/me
                                                                └ 設定 /app/me/settings
タブ外
  /app/new          記録（一画面一問・下タブ無し）
  /app/record/[id]  記録ひとつ＋振り返り
  /app/journey      道のり（「今日」の線から入る。タブに置かない）
  /app/start        はじめ方（下タブ無し）
```

道のりをタブに置かないのは、置いた瞬間にそこが「進捗を見に行く場所」になるから。
